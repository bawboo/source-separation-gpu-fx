# Changelog

本專案的版本紀錄。格式依循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，
版本號依循 [語意化版本](https://semver.org/lang/zh-TW/)。

## [0.0.12] - 2026-09-20

### 變更

- **視窗改成標準三鈕（最小化／最大化／關閉），並且隨時可以拉大縮小。** 進階面板裡的
  「全螢幕」與「縮放介面」兩個按鈕移除：全螢幕由右上角的最大化按鈕取代，縮放不再需要先
  勾選，直接拖視窗邊緣或右下角即可。內容等比縮放並置中，最大化時左右留邊而不是變形；
  切換一般／進階面板會沿用目前的縮放比例，但不會超出螢幕

## [0.0.11] - 2026-09-15

### 變更

- **一般面板的品質選擇不再跟進階面板共用狀態。** 先前兩者是同一個東西：在進階面板
  換過模型，回到一般面板按匯出跑的就是那個模型，而一般面板上沒有任何東西顯示它。
  一個只是好奇進去看看的使用者，回來按下匯出得到的會是他沒有選的模型。現在兩邊完全
  獨立，一般面板的選擇在工作開始的那一刻才套用，進階面板做什麼都不會反向影響它
- **兩個選項改名並重新定義**：「高品質」＝HTDemucs、「超高品質」＝卡拉OK模型。
  提示文字改為「分離品質極好，一般情況下不需要超高品質。」與「更好的伴奏分離，
  甚至可以保留和聲。」
- **CPU 版的「超高品質」回到不可選**，滑鼠移上去說明原因（沒有 GPU 時一首 4 分鐘的歌
  約 1 小時）。0.0.10 曾改成可選並顯示警告，但那個設計的前提是使用者會讀提示；
  一個要跑一小時的按鈕按下去之後就沒有回頭路了

### 修正

- **0.0.10 的 Windows 安裝檔與免安裝包裡裝的是前一天的 App**，因此那一版實際上**沒有**
  它自己宣稱的品質切換功能。打包安裝檔的腳本從不自己重做 payload，只檢查 payload
  資料夾「存不存在」——一份前一天留下的 payload 看起來跟當天的一模一樣。所有測試與
  發行前檢查都通過，因為它們驗的是原始碼建出來的東西，沒有人驗打包進去的是哪一份。
  現在安裝檔與免安裝包的打包都會比對 payload 裡的 App 與剛建好的 SHA-256，不一致就
  中止並印出兩邊的雜湊與時間
- **兩顆品質按鈕都會在滑鼠移上去時說明自己用的是哪個模型**

- **換模型不會作廢已經分離好的結果**，所以「用超高品質跑完→切回高品質→按匯出」會把
  前一個模型的輸出當成這一個存出去。現在只有品質真的改變才重跑；按另一顆匯出鍵仍然
  沿用暫存，不會重跑分離（暫存在匯入新檔案時清除，這部分行為不變）

## [0.0.10] - 2026-09-15

### 新增

- **一般面板多了「一般／高品質」兩顆品質選擇鍵。** 一般＝HTDemucs 4 軌，高品質＝
  RoFormer 卡拉OK（會保留和聲，只拿掉主唱）。提示文字附上以本機實測為準的預估時間，
  一首 4 分鐘的歌：CUDA 10 秒／4 分鐘、Apple Silicon 1 分鐘／45 分鐘、CPU 3 分鐘／50 分鐘。
  一般情況下 HTDemucs 就分得夠好，提示裡也這樣寫
- **高品質在每台電腦上都可以按，慢的機器改成警告而不是擋住。** 一度在 CPU 上變灰，
  前提是它要跑「一個半小時以上」；量出來是 50 分鐘，跟從來沒被擋過的 Apple Silicon
  的 45 分鐘是同一個量級——擋一個放一個，對著兩台機器的人解釋不通。慢的機器改成把
  真實數字用警示色寫在按鈕旁邊，讓使用者自己決定
- macOS（Apple Silicon）移植：universal binary（`arm64;x86_64`）、per-architecture 的
  frozen runtime、ad-hoc 簽章，以及 POSIX 共享記憶體 IPC 的單獨驗證工具

### 修正

- **在進階面板換過模型後，回到一般面板會繼續用那個模型跑。** 一般面板只說得出「一般」
  與「高品質」兩種狀態，其他模式回去之後畫面上沒有任何東西顯示它，於是一首 4 分鐘的歌
  在使用者以為自己選的是一般模式時跑了五十分鐘。現在離開進階面板時，若當前模式不是這兩
  種之一就會退回「一般」
- **一般面板在分離中沒有取消鍵**，遇到上面那種狀況只能等它跑完。取消鍵現在兩個面板都有
- **Apple Silicon 上的預估時間顯示的是 NVIDIA 的數字。** 先前只分 CPU 與非 CPU，MPS 被
  歸進「非 CPU」，所以 M1 上的高品質模式寫著「約 3 分鐘」，實測是 41 分鐘
- **macOS 版包裡有兩個二進位要求 macOS 26，實際上是我們自己建壞的。** 建 FFmpeg 時
  沒有指定最低系統版本，clang 就把建置機的版本蓋上去，而 macOS 會拒絕載入最低版本
  比自己新的二進位——使用者一匯入檔案就會失敗。掃過發行包裡全部 441 個執行檔後，
  那兩個是唯一要求最新系統的
- **Intel 版的最低系統從 macOS 14 降到 12。** scipy 從 1.14 起的 wheel 被標成最低 14.0，
  而 macOS 14（Sonoma）砍掉了 2018 年以前的所有 Intel Mac——也就是說這個為「沒有
  Apple Silicon 的機器」而存在的版本，正好在其中一大半上跑不起來。改釘 scipy 1.13.1
  （標的是 10.9）之後，Intel 版涵蓋到 macOS 12 Monterey。Apple Silicon 版的下限仍是
  macOS 14，那是 torch 自己的 wheel 決定的，換不掉
- **每一份 macOS 發行包宣告的最低系統，改成它自己真的做得到的那個。** 先前兩份都寫
  12.0，但 Apple Silicon 版做不到——torch 的 arm64 wheel 就是 14.0，換不掉。差別不在
  數字而在失敗的樣子：停在 Monterey 的 M1 上，App 會開起來、吃得下檔案，然後在背景
  死掉，看起來像「分離功能壞了」。現在 Intel 版宣告 12.0（實測就是）、Apple Silicon 版
  宣告 14.0，太舊的系統在啟動時就會被擋下並說明原因。**行為改變**：macOS 12／13 的
  Apple Silicon Mac 先前開得起來（然後失敗），現在開不起來
- **macOS 發行包改成 DMG**，內含 App、Applications 捷徑，以及一份說明第一次開啟要怎麼
  通過系統安全檢查的中文文件
- **Apple Silicon 上「自動」會挑一個跑不動這個模型的裝置。** 判斷依據是
  `mps.is_available()`，但可用不等於做得到：Intel Mac 唯一能用的 torch 2.2.2 回報
  MPS 可用，卻缺 RoFormer 需要的 STFT，於是分離直接崩潰。現在改成實際跑一次那個
  模型真正要用的運算再決定
- **RoFormer 從來沒有 MPS fallback 保護。** `PYTORCH_ENABLE_MPS_FALLBACK` 設在
  HTDemucs 的模組裡，而 RoFormer 不經過那條路。搬到兩個後端共用的進入點
- **高品質模式的預估時間有兩格是沒有出處的數字。** CUDA 寫 3 分鐘、實測 3.6 分鐘；
  CPU 寫 97 分鐘（介面上顯示成「一個半小時以上」）、實測約 46 分鐘，高估了一倍以上。
  現在每一格量的都是使用者實際會跑到的那個模型（一般＝htdemucs、高品質＝
  karaoke-gabox），各自取整往上留一點餘裕。一個高估的數字和一個低估的一樣糟：
  低估讓人措手不及，高估讓人放棄一個其實可用的功能
- **Apple Silicon 上的 RoFormer 分離一直跑在 CPU。** 運算裝置選「自動」（預設值）時
  我們把 `auto` 原封不動交給上游，而上游的解析只認得 CUDA，找不到就掉回 CPU——MPS
  從頭到尾沒被看見。現在 `auto` 在送進上游之前就由 worker 自己解析。加速依模型而定：
  在整首 246.7 秒的檔案上，guitar 從 461 秒降到 198 秒（約 2.3 倍，比即時還快），
  但高品質用的 karaoke-gabox 只快 7%，所以它的預估時間不變。MPS 與 CPU 的輸出逐樣本
  最大差 2.8e-05（float32 核心的捨入差異），8 GB 機器上峰值 4.04 GB、無 OOM
- **六軌分離的伴奏漏掉部分音軌**：組伴奏時只扣掉人聲，而不是把其餘每一軌都加回來
- CPU runtime 啟動時會還原一個它跑不動的模式（例如 Crowd）——還原時沒有檢查可用性

## [0.0.9] - 2026-09-14

### 新增

- **分離模式與音軌推桿都標上中文意思**，例如 `Vocals（人聲）`、`Dereverb（去混響）`、
  `Residual（其餘）`。模型圈子用英文命名，App 照樣顯示，但那些字對中文使用者沒有說明
  效果。中文是附加而非取代，英文介面維持純英文
- macOS 建置工具鏈：`tools/macos_build_everything.sh` 一個指令從零建到可執行的 `.app`，
  含 LGPL FFmpeg 自行建置、兩種架構的 runtime 凍結與封裝、ad-hoc 簽章。
  完整說明見 `docs/MACOS_BUILD.md`，移植的交接說明見 `docs/MACOS_HANDOFF.md`

### 修正

- **快速匯出（僅匯出人聲／伴奏）改為套用輸出增益**，與預覽播放一致。先前預覽會套用而
  匯出不會，所以聽到的音量不等於匯出的音量。音軌推桿不納入（單軌匯出不是那些推桿描述的
  混音）；維持預設 0 dB 時匯出結果與先前完全相同，逐軌匯出仍維持原始音量
- **進階面板的「分離」鍵會因為預設檢查點沒裝而按不下去**：與 0.0.7 修掉的匯出鍵是同一個
  錯誤，只是漏改了這一顆。而且它檢查的是 HTDemucs 模型下拉選單裡的模型——選 RoFormer
  模式時那個模型根本不會被使用。改成不再用檢查點擋，按下去缺什麼就自動下載

### 其他

- commit 身分改用 GitHub noreply 信箱；既有歷史刻意不重寫（會讓七個已發布 tag 失效）
- 根目錄整理：四份已被取代的文件移入 `docs/history/`；README 修正了安裝版下載量、
  RoFormer 套件安裝方式與 macOS 章節

## [0.0.8] - 2026-09-14

### 變更

- **runtime 改由 CDN 提供，並改用 7z/LZMA2 壓縮**。實測同一條線路上，GitHub Release
  的下載只有 0.42～0.67 MB/s（換另一個知名 repo 的 release 也一樣，所以是 GitHub 的
  服務端而非本專案），而 Hugging Face 與 Cloudflare 都有 8～10 MB/s。runtime 因此改放
  `huggingface.co/BAM/music-ssp-fx-runtime`，檔案置於版本化路徑；安裝程式仍逐檔驗
  SHA-256，所以「舊安裝檔不會抓到新 runtime」這個保證沒有變弱
- 壓縮格式由 ZIP deflate 換成 7z/LZMA2：CPU runtime 264.6→178.5 MB（−33%）、
  CUDA runtime 2631.3→1644.1 MB（−38%）
- **實測 GPU 版完整安裝：約 95 分鐘 → 4.5 分鐘**（下載 173 秒、解壓與寫檔 96 秒、
  安裝後自我測試 3 秒）

### 修正

- **安裝程式的五個對話框會卡住無人值守安裝**：Inno 的 `MsgBox` 不受 `/SUPPRESSMSGBOXES`
  影響，實測下載量確認那一個把一次靜默 GPU 安裝卡了 460 秒——比它要確認的那段下載還久。
  全部改用 `SuppressibleMsgBox` 並給定預設答案：下載確認與 CPU 後備通知取「是」，
  自我測試失敗取「否」（無人值守時寧可明確失敗，也不要留下壞掉的安裝），
  解除安裝時「是否一併刪除模型與設定」取「否」（不問就不刪使用者資料）
- **進階面板的「分離」鍵也會因為預設檢查點沒裝而按不下去**：與 0.0.7 修掉的匯出鍵是同一
  個錯誤，只是漏改了這一顆。而且它檢查的是 HTDemucs 模型下拉選單裡的模型——選 RoFormer
  模式時那個模型根本不會被使用，等於為了一個不會被開啟的檔案停用按鈕。改成不再用檢查點
  擋：按下去要嘛開始跑（缺的權重自動下載），要嘛把真正的原因寫在狀態列

## [0.0.7] - 2026-09-12

### 修正

- **普通面板的兩個匯出鍵會因為預設檢查點沒裝而變成死路**：啟用條件要求 htdemucs 檢查點
  已安裝，但普通面板上根本沒有任何模型控制項，也沒有任何文字說明為什麼按鍵是灰的。改成
  不再用檢查點擋按鍵——按下去就跟 RoFormer 模式一樣自動下載缺少的權重再繼續；狀態列也會
  先說明「第一次會先自動下載 84 MB 的預設模型」。下載期間保留該匯出請求，下載完自動接上
  分離與匯出
- **普通面板的提示叫使用者按一個它沒有的按鈕**：匯入完成後狀態列寫「請按下「分離」」，
  但普通面板上沒有分離鍵。提示改為跟著目前的面板走：普通面板說「請按下「僅匯出人聲」或
  「僅匯出伴奏」」，進階面板才說「請按下「分離」」
- **永遠無法完成的快速匯出請求會一直卡住普通面板**：該請求沒被清掉時，兩個匯出鍵與切換
  面板鍵會被停用到關閉程式為止；而匯入鍵並不受它限制，所以症狀正是「匯入完成後匯出鍵按
  不下去」。現在只要沒有東西在跑、或分離不可能再產生預覽，就釋放該請求
- **安裝檔無法編譯**：新增的下載量確認在 `InstallCudaRuntime` 定義之前就呼叫它，Inno 的
  Pascal Script 依順序解析識別字，因此 564c92e 那份狀態做不出任何安裝檔
- **RoFormer 雙軌模式的推桿標籤與實際軌別對調**：worker 每軌輸出一個檔案，程式依檔名
  判斷軌別，而檔案是照字母序讀入的（`<歌名>_instrumental.wav` 排在 `_vocals.wav` 前面），
  所以第 0 軌其實是伴奏；但進階面板照分類名稱固定把第 0 軌標成「Vocals」。結果把另一支
  推桿拉掉後留下的是整首伴奏，聽起來像完全沒分離。標籤改為依分離結果實際回報的軌別命名
  （且只在結果來自目前選定的模型時採用）。影響所有雙軌模式：人聲、伴奏、卡拉 OK、
  去混響、去噪、群聲、氣音、吉他。匯出檔案本來就依軌別挑選，不受影響
- 進度條在不確定狀態時，階段文字被跑馬紋蓋住看不清楚；完成時的百分比也曾因為
  `setTextToDisplay()` 永久關閉百分比顯示而消失

### 變更

- **安裝時就講清楚要下載多少、大概多久**：選擇頁的「自動偵測」會直接說這台電腦會裝哪一種
  與下載量，三個選項都標出實際大小（由打包時的封存檔大小推導，不再是寫死的數字），
  GPU 選項另標預估時間；按下一步前會再確認一次（含續傳說明），摘要頁也列出下載量
- **進度條會說明現在在做什麼**：下載模型／匯入檔案／載入模型／分離中／匯出人聲、伴奏、
  各軌、混音、寫回影片／批次分離與批次匯出（批次另顯示第幾個／共幾個）。之前分離完接著
  匯出時，同一條進度條再跑一次，看起來像同一件事重跑
- **CPU 版把所有分離模式開回來**。實測所有 RoFormer 模型在 CPU 上落在同一個時間帶
  （30 秒片段 4 到 14 分鐘），與 checkpoint 大小幾乎無關（吉他 43 MB 與氣音 797 MB 相近），
  因為成本來自 transformer 的序列長度而非參數量。既然吉他本來就提供，其餘沒有理由隱藏；
  CPU 模式的警告改為具體時間
- **預設分離模式改為 HTDemucs 4 軌**（兩個版本一致）。之前預設是 RoFormer 人聲類別，
  但 CPU 版會過濾掉該模型而退回 4 軌，等於同一個預設在兩種安裝上行為不同

## [0.0.6] - 2026-09-10

### 變更

- **介面外觀重整，操作步驟一眼可辨**。全新的深色配色與扁平圓角控制項；顏色只用來
  表達「這個按鈕做什麼」：藍色是主要動作（匯入／分離）、琥珀色是人聲、青色是伴奏、
  紅色是錄音與取消，停用的控制項以淡出表示。簡易面板新增「1 匯入檔案 → 2 分離 →
  3 匯出」步驟列，會隨目前進度標示完成／進行中；狀態列左側新增狀態圓點（灰＝閒置、
  藍＝進行中、綠＝完成、紅＝錯誤／錄音中），不用讀字就知道 App 在做什麼。版面尺寸
  不變（560×260／720×576／720×826）。
- **匯出後一鍵開啟輸出位置**：任何匯出（人聲／伴奏、逐軌、混音、MP4 回填、批次）完成後，
  狀態列右側出現「開啟輸出位置」，在檔案總管中選取剛寫出的檔案或資料夾，不用再照著
  狀態列的路徑去找
- **預覽列顯示波形總覽**：分離完成後，進階面板的預覽位置滑桿下方畫出整首的波形
  輪廓，已播放的部分用藍色標示，拖動滑桿時一眼就能對到歌曲段落
- **把檔案拖進視窗就能匯入**：拖曳時整個視窗亮起藍框並顯示「放開即匯入」，放開一個
  檔案就取代目前的檔案，放開多個就成為批次；在即時模式下拖入會自動切回錄音模式。
  檔案列的提示文字也改為告訴使用者可以拖曳。分離、匯出或下載進行中時拖入會被忽略並
  在狀態列說明原因（否則會像舊的匯入路徑一樣默默取消進行中的分離）
- 匯入被拒時一定說明原因：路徑超過 260 字元（Windows 檔案 API 的上限）會提示「把檔案
  移到較短的資料夾」，而不是「找不到檔案」；匯入／匯出進行中再匯入會提示等待，
  之前是默默不動作
- 鍵盤快捷鍵：空白鍵試聽／暫停、Esc 取消進行中的工作、Ctrl+O 匯入、Ctrl+E 匯出，
  只在對應按鈕可用時生效，提示文字也標出快捷鍵
- **批次匯入全部失敗時 App 會卡在「載入中」**：多檔匯入的狀態在啟動時設為載入中，只有
  第一個成功的檔案會把它推進；若一個都讀不了，狀態永遠停在載入中，所有按鈕停用、
  取消也無效，只能重開。現在改為進入錯誤狀態並說明
- **回填 MP4 的錯誤處理**：只有「MP4 裝不下這種編碼」才會重新編碼，磁碟已滿或檔案
  被鎖住之類的失敗直接回報原因，不再白跑兩次重新編碼；中途取消會說「已取消」而不是
  「編碼不相容」；多次嘗試的錯誤訊息保留第一個而不是只剩最後一個
- 多檔批次的剪輯列最多顯示 8 列，更多時在列表內捲動，視窗不再無限增高
- 拖放：拖進來的項目若不是真實檔案（例如壓縮檔或手機裡的內容）會提示；批次進行中或
  已排程的快速匯出期間拖入會被擋下；實際匯入改在放開後的下一個訊息迴圈執行
- 空白鍵與 Ctrl+O 只在對應按鈕顯示在畫面上時生效（簡易面板不再暗中播放看不見的預覽）
- 暫時提示不再蓋住之後出現的錯誤訊息；狀態列的 tooltip 與文字同步；「開啟輸出位置」
  只在有匯出時佔用狀態列空間，且不再每 0.1 秒對匯出路徑做磁碟查詢（網路磁碟斷線時
  會卡住介面）；匯入新檔後按鈕消失；批次匯出的檔名只在真正開始匯出後才登記
- 接受的媒體副檔名清單集中到一處（`htfx::kAudioExtensions`／`kVideoExtensions`），檔案
  選擇器、拖放、匯入判斷與測試都從它推導；之前四處各有一份
- 分離前預覽滑桿保留一條軌道；路徑過長的提示只在 Windows 出現；語言設定改為原子變數
  （工作執行緒讀、介面執行緒寫）
- 批次匯入時讀不了的檔案會在狀態列點名（「已匯入 2 個檔案；1 個無法讀取，已略過：x.wav」），
  之前只是默默少一個
- 剪輯列的「待分離／分離中／完成／失敗」改為隨介面語言即時切換（之前是匯入當下的語言）
- 批次匯入後的狀態列改為告訴使用者簡易面板上該按哪個按鈕（之前寫「按分離」，但簡易
  面板沒有分離按鈕）
- 主要按鈕與分離模式選單加上滑鼠提示，說明各自會做什麼（例如「僅匯出人聲」會先
  自動分離）

### 修正

- **在舊安裝上換另一種 runtime 會讓自我測試必定失敗**（0.0.5 安裝失敗的真正原因）：
  安裝程式把 runtime 壓縮檔直接解壓到安裝目錄，先前另一種版本留下的檔案（例如 CUDA 版的
  `c10_cuda.dll`）沒有清掉，新的 CPU 版 torch 啟動時載入到舊 DLL 就以 WinError 127
  失敗。現在安裝前先清空 `Resources\sidecar\Runtime`
- **安裝程式的「安裝後自我測試失敗」不再是死路**：0.0.5 的安裝程式只看 worker 的結束碼，
  失敗時只顯示「self-test failed」就中止安裝，使用者無從得知原因。現在（1）worker 失敗
  時也會寫出報告（狀態、失敗階段、錯誤訊息、torch／CUDA 版本），安裝程式把 worker 的
  輸出存到 `%LOCALAPPDATA%\Music SSP FX\Logs\install-self-test.log`；（2）GPU 自測失敗
  會自動改測 CPU，通過就以 CPU 完成安裝並說明原因（例如驅動太舊），不會整個安裝失敗；
  （3）兩者都失敗才詢問是否仍要完成安裝，訊息包含實際錯誤與記錄檔路徑。另新增
  `/RUNTIME=cpu|cuda|auto` 與 `/SELFTESTDEVICE=` 命令列參數供靜默安裝與診斷
- **WebM／WMV／MPEG 來源的影片無法回填成 MP4**：混音回填一律對影片串流做 stream
  copy，而 MP4 容器裝不下 VP8、WMV2、MPEG-1 這些編碼，ffmpeg 直接失敗。現在 stream
  copy 失敗時自動改為重新編碼影片（先 OpenH264，再退到內建的 MPEG-4 編碼器，兩者都在
  隨附的 LGPL FFmpeg 內），狀態列會顯示「正在重新編碼影片」
- **切換語言後整個面板變成「尚未選擇模式」**：重貼下拉選單文字只改了選單項目，
  顯示文字沒更新，而 JUCE 的 `getSelectedItemIndex()` 以顯示文字比對，於是分離模式
  與錄音／即時模式都被讀成未選擇，音量推桿、模型選單全部停用，直到使用者重選為止。
  現在重貼後以 id 重新選取。`ui_configuration_smoke` 新增對應斷言
- **英文介面的「—」「·」「…」顯示成亂碼**：字串表的英文字面值經 `juce::String(const char*)`
  以 ASCII 解碼。全部改為 UTF-8 字面值
- 進度條在 100% 時看不到百分比：自訂進度條繪製，文字帶陰影，在軌道與填色上都可讀
- 狀態列的錄音狀態（準備就緒／錄音中／分離中／分離完成…）改為依介面語言顯示，
  之前一律是英文
- 多檔批次時視窗會隨剪輯列數增高：之前面板高度固定，每多一個檔案就把進度條與狀態列往下
  擠出面板，三個檔案以上就看不到狀態
- 進階面板的隱藏列不再留白：RoFormer 模式只顯示兩軌推桿時，其餘四列與 Demucs 模型列
  不再佔位，版面隨模式緊縮
- **批次匯出時同名不同格式的檔案會互相覆蓋**：`song.mp3` 與 `song.flac` 一起批次
  匯出，兩者都寫到 `song_accompany.wav`，最後只剩一個。同一批內名稱重複時，第二個
  起加上來源副檔名（`song (flac)_accompany.wav`），再重複則加計數

### 測試

- 新增 `htdemucs_format_matrix_check`：對檔案選擇器接受的每一種格式（WAV／FLAC／
  AIFF／MP3／OGG／M4A，MP4／MOV／MKV／AVI／WebM／M4V／WMV／MPEG）與特殊取樣格式
  （8／24 位元、32 位元浮點、單聲道、22.05／48／96 kHz、5.1 聲道、1080p60 影片）
  各跑一輪「匯入→長度核對→4 軌分離→人聲／伴奏／逐軌／混音匯出→影片回填 MP4」，
  外加必須乾淨拒絕的壞檔（非媒體、零樣本、無音軌影片）、0.5 秒短檔、截斷的 MP3、
  含中文與空白的路徑、跨格式批次、匯入中途取消。第二批邊界案例再加上 8 kHz／192 kHz、
  7.1 聲道、180 字元檔名（完整路徑超過 260 字元）、VP9+Opus、HEVC、MJPEG+PCM、雙音軌
  MKV、影片比音訊長、0.05 秒短檔、20 分鐘的長檔（GPU 分離 59 秒；並在它匯出到一半時
  取消，必須乾淨結束、不留半成品或暫存檔）、匯出到不存在的磁碟機（必須報錯、
  不得卡住、之後仍能正常匯出），以及使用者亂序操作：試聽中匯入、解碼中再匯入
  （必須拒絕並說明）、批次裡夾一個非媒體檔（其餘照常）、以 RoFormer 模型跑批次；
  `--roformer <id>` 可讓整套逐檔檢查改走 RoFormer（kim-vocals 對全部 28 個檔案的
  分離、逐軌、混音與 MP4 回填 233/233，GPU 15 分鐘；guitar 模型在 CPU 凍結 runtime 上
  33/33，每個 30 秒片段約 205 秒）；`--batch N` 可把批次段落放大到
  12 個檔案。最終版本的結果：GPU 253/253（含 12 檔批次與全部壞檔的批次），CPU 凍結 runtime
  全部 28 個檔案 235/235（52 分鐘）。測試資料為 30 秒合成訊號，由腳本產生，不需要真實歌曲

## [0.0.5] - 2026-09-04

### 變更

- **模型清單精簡為每種分離一個代表性權重**。RoFormer 從 99 個模型縮到 8 類 9 個
  （`assets/models/roformer-catalog.json`）：每類保留一個 audited 代表，只有效果
  確實不同時才多留一個（去噪類另保留「串音抑制」）；「general」與「instvoc」兩類
  在效果上就是人聲／伴奏兩軌分離，整類移除。HTDemucs 只保留 4 軌（`htdemucs`）與
  6 軌（`htdemucs_6s`），`htdemucs_ft`／`hdemucs_mmi` 屬品質分級而非不同效果，
  不再列出。完整 manifest 保留不動，checkpoint 網址與 SHA-256 仍可驗證
- **CPU 版只提供實測跑得動的模型**：目錄檔每個條目帶 `cpu` 旗標，主程式讀取
  runtime 旁的 `runtime-manifest.json` 得知安裝的是 CPU 還是 CUDA runtime，CPU 版
  不列出被標為 `cpu: false` 的模型。整首實測（CPU runtime，5 分鐘的歌）：HTDemucs
  4／6 軌約 2 分鐘；45 MB 的 guitar 模型推論約 8 分鐘；456 MB 的 de-reverb 估計約
  4 小時；913 MB 級更慢。因此 CPU 版的 RoFormer 只提供 guitar，其餘標為 GPU 專用；
  CPU 上的人聲／伴奏分離由 HTDemucs 4／6 軌承擔

### 修正

- **全新安裝時 guitar／denoise／crowd 三個模型載入必定失敗**：快取器只預先下載並
  驗證 checkpoint，config yaml 交給上游套件自己抓，而上游對這三個模型用的 config
  網址已失效（404）。開發機能跑只因舊快取裡早就有 config。manifest 本來就記錄了
  可用的 `config_url` 與 `config_sha256`，現在快取器連 config 一起下載並驗證，
  上游看到檔案存在就不再嘗試自己的網址
- **模型下載一遇連線重設就整個分離失敗**：下載 1.5 GB 的 checkpoint 途中被遠端
  重設（10054）沒有任何重試。下載器改為以 HTTP Range 從已下載的部分續傳，並以
  指數退避重試最多 6 次
- **批次進行中沒有可查詢的狀態**：批次分離／匯出是逐檔驅動 media 執行緒，檔與檔
  之間 `isMediaBusy()` 會短暫為 false，UI 或呼叫端據此判斷「已完成」會太早。
  新增 `isBatchBusy()`，整個批次迴圈期間為 true（在啟動執行緒**之前**就設起，
  否則呼叫端緊接著查詢仍會看到 false）
- **啟動時清理暫存目錄會誤刪另一個實例正在用的目錄**：0.0.4 加入的啟動清理會刪掉
  所有 `cpp-route-*`，若同時開著第二個實例（或測試與 App 並行），對方進行中的分離
  會失敗。改為只清理修改時間超過一小時的目錄

### 測試

- 新增 `htdemucs_full_feature_check`：對一首真實歌曲跑完**所有**使用者功能——
  匯入、每一種分離模式、人聲／伴奏快速匯出、逐軌匯出、混音匯出、中途取消、
  影片匯入與 MP4 回填、雙檔批次匯入／分離／匯出——並檢查每個輸出都是 44.1 kHz
  立體聲 float、長度與來源一致、非靜音。以 `--backend cpu|auto` 分別驗收兩份 runtime
- 逾時改為「多久沒有任何進度」而非總時長：模型下載或慢速推論只要持續回報就不算
  卡住。原本 20 分鐘的總時限會把「下載 800 MB ＋ 分離」誤判為失敗
- smoke tests 的模型數量改由目錄檔推導，不再硬編碼 99 個模型
- 修正 `ui_configuration_smoke` 間歇性的 Access violation：測試在開頭蒐集一次
  元件指標，之後編輯器重建子元件時舊指標懸空。語言檢查前改為重新蒐集元件樹，
  並加入 SEH 崩潰回報（帶符號的堆疊），日後同類問題可直接定位

## [0.0.4] - 2026-09-01

### 修正

- **RoFormer 分離期間完全沒有任何進度回報**：Python worker 從頭到尾只在最後
  印一次結果，C++ 端雖然有讀取 worker 的輸出，卻只把它當診斷文字累積起來。
  `setSeparationMessage()` 在啟動時設定一次之後就再也不更新，進度條固定是
  「不確定」動畫——於是整段流程（啟動、下載模型、載入、推論）都只顯示
  「正在載入 <模型>」，和當機無法區分。實測一首 5 分鐘的歌：啟動與 CUDA
  初始化 25 秒、載入模型 3 秒、推論 57 秒，**前 28 秒完全沒有任何回饋**，
  而模型未快取時還要再加上數百 MB 的下載
- worker 現在回報 prepare／download／load／infer／verify 五個階段，下載階段
  帶實際百分比；C++ 端解析這些標記，同時解析上游本來就每秒輸出、但過去被丟棄的
  「Estimated time remaining」，據此顯示剩餘時間並驅動進度條
- **worker 的輸出被緩衝，讓進度「憋著跳」**：把上述訊息接起來之後，剩餘時間
  整段只更新 1 次——訊息確實有送出，但全部卡在管線緩衝裡直到程序結束才抵達，
  使用者看到的仍然是長時間不動、然後突然跳到 81%。改成 line buffering 也無效，
  因為上游是 `sys.stdout.write(f"...
")`，結尾是歸位字元而非換行，永遠等不到
  觸發 flush 的 `
`。改為完全不緩衝後，同一首歌的估計值從 1 次變成 62 次、
  平均間隔 0.88 秒，進度條才真正逐秒推進
- **主程式讀取 worker 輸出的粒度，讓進度再次卡住**：worker 端修好之後，
  凍結版的進度**仍然**只更新 2 次（同一份 worker 直接用 Python 讀是 62 次）。
  `juce::ChildProcess::readProcessOutput` 會忙碌等待**直到填滿呼叫端給的整個
  緩衝區**才返回；原本給的是 4 KB，而每則進度約 40 bytes，等於要累積約 100 則
  ——比整段分離產生的還多——所以全部積到程序結束才一次抵達。改用 64 bytes 的
  緩衝後，同一首歌的進度更新從 2 次變成 **93 次**，剩餘時間逐秒遞減。
  順帶把斷行改為以原始位元組切割：一次讀取可能停在多位元組字元中間，先解碼
  再切行會弄壞該字元
- **每次 RoFormer 分離洩漏約 300 MB**：每次執行都建立
  `cpp-route-<uuid>/`，裡面放一份完整的輸入 WAV 與所有分離結果，而且從來不刪。
  開發樹已累積 8.7 GB。stems 讀進記憶體後這些檔案就沒有用途，現在改由 scope
  guard 確保刪除（成功、失敗、取消都會清），並在啟動時掃除舊版留下的殘留
- **輸入所在資料夾裡的其他 WAV 會被一起分離**：worker 把
  `input_path.parent` 整個交給上游的 `infer()`，它會處理該資料夾下所有音檔。
  旁邊多一個檔案就讓分離時間加倍，而且處理第二個檔案時會踩到上游
  `_print_estimate` 的 `AttributeError: 'chunk_size'` 而讓整個 worker 崩潰。
  現在把輸入隔離到只含它自己的暫存資料夾（優先用硬連結，跨磁碟才複製）
- 「正在啟動 Demucs worker」這則訊息對兩種後端都會顯示，選 RoFormer 模式時
  看到 Demucs 字樣會誤導，改為中性的「正在準備分離…」

### 測試

- `goal_check` 在等待期間會印出狀態文字的每次變化與百分比——先前它只印最終
  結果，因此「整段沉默」這種缺陷它根本測不出來

## [0.0.3] - 2026-08-30

### 變更

- **缺少的 HTDemucs 模型改為按需下載**：在全新安裝上選「6 軌分離」會被擋下來，
  訊息要你自己去「進階選項」下載模型；但選任何 RoFormer 模式卻是自動下載直接跑。
  兩個模型家族的行為不一致，而 6 軌分離是 README 首頁就宣傳的功能，新使用者
  第一次點就撞牆。下載器本來就存在也能用，只是沒有接到分離流程上——現在缺模型
  會自動開始下載（一樣驗證固定的 SHA-256），下載完成後自動接著分離。若使用者
  在下載期間換了模型，排隊中的分離會被放棄而不是突然開始；下載失敗則讓分離進入
  錯誤狀態，不會無限等待

### 修正

- **`goal_check` 從未 pump 過 message queue**：它有 `ScopedJuceInitialiser_GUI`
  但不跑 dispatch loop，所以任何依賴 `MessageManager::callAsync` 的行為在它裡面
  完全測不到——本次改動的第一版正好踩中，看起來會動但實際上永遠不會續跑。
  續跑改為直接在下載執行緒執行（`beginSeparation()` 不碰任何 UI），如此在沒有
  dispatch loop 的 host 或工具中行為一致；`goal_check` 也改為會等待進行中的下載，
  而不是把 `beginSeparation()` 回傳 false 直接判定失敗

## [0.0.2] - 2026-08-30

更名為 **Music SSP FX**，並把 RoFormer 併進既有的 frozen runtime，讓使用者完全
不必安裝 Python。

### 變更

- **產品更名**：`HTDemucs GPU FX` → `Music SSP FX`（README 標題為
  Music Source Separation FX）。設定與 sidecar 目錄改用新名稱，舊目錄仍會被讀取
  並在首次啟動時搬移，既有使用者的語言與啟動選擇不會遺失
- **一份 runtime、兩個後端**：HTDemucs 與 MelBand RoFormer 共用同一份 PyTorch，
  凍結成單一 PyInstaller bundle（`worker/worker_main.py` 依第一個參數分派）。
  另外凍結一份 RoFormer runtime 會讓 torch 與 CUDA 函式庫重複約 2.5 GB
- **RoFormer 不再需要 Python**：偵測到 frozen runtime 時走它的 `roformer`
  子命令；找不到時才退回開發用的 python + script 路徑
- **安裝程式的 runtime 選擇頁**：改為三個選項——自動偵測（預設）、CPU、GPU
  (CUDA)。該頁一律顯示（過去偵測不到 CUDA 時整頁被跳過），並顯示 DXGI／CUDA
  Driver API 的偵測結果；在沒有可用 CUDA 的機器上手動選 GPU 會先確認
- 授權通知改為從凍結該 runtime 的 Python 環境動態收集
  （`tools/collect_runtime_licenses.py`），取代寫死且已過時的版本清單

### 發行體積

| 包 | 0.1.0（不含 RoFormer） | 0.0.2（含 RoFormer） |
|---|---|---|
| CPU | 297.6 MiB | **283.5 MiB** |
| CUDA（合計） | 2.31 GiB（2 個檔） | 2.59 GiB（3 個檔） |

CPU 版含了 RoFormer 反而更小（新的 CPU PyTorch wheel 較精簡）；CUDA 版
增加約 295 MiB，主要是 librosa 的相依（llvmlite 70 MB、scipy 37 MB）。
未壓縮的 RoFormer 淨增量為 110 MB。

### 修正

- CUDA runtime 的分包寫死了 `torch_cuda.dll`／`cudnn_cnn_infer64_8.dll`／
  `cublasLt64_12.dll` 三個檔名，換成新的 PyTorch/CUDA 版本後檔名不同會直接失敗；
  改為依實際檔案大小動態分包（每包上限 1.7 GB，低於 GitHub 的 2 GB 單檔限制），
  安裝程式的 `[Files]` 也改為由建置腳本產生，不再限定兩個檔案
- PyInstaller 工具目錄改為依直譯器版本區分，CPU（3.11）與 CUDA（3.13）runtime
  不再互相污染
- **HTDemucs 4 軌在凍結後完全無法使用**：該 checkpoint 是以 NumPy 1.x pickle 的，
  反序列化時會引用 `numpy.core.multiarray`，而 NumPy 2 只把該路徑保留為相容 shim，
  PyInstaller 的靜態分析看不到它，`torch.load` 因此丟出 ModuleNotFoundError。
  6 軌 checkpoint 格式較新所以不受影響，只有 4 軌會壞。已加
  `--collect-submodules numpy.core`。此問題只有在實際跑 frozen runtime 時才會
  出現，走開發用 Python 環境的測試一律看不到

- **安裝版的所有 RoFormer 模式都會失效**：模型快取與工作目錄的預設值是開發樹的
  相對路徑（`cwd/../verify/...`），安裝或免安裝執行時該處不存在、在 Program Files
  下也不可寫，`beginSeparation()` 因此直接判定路徑不可用。改為解析到
  `%LOCALAPPDATA%\Music SSP FX\` 之下，並在首次使用時建立
- **安裝包遺漏 `roformer-manifest.json`**：沒有它，模式清單退化成只剩 HTDemucs
  4／6 軌，99 個 RoFormer 模型全部不出現
- **frozen runtime 內含建置機器的路徑**：pip 的 `direct_url.json` 會記錄本地或
  VCS 安裝來源。建置腳本現在會刪除它，並掃描整個 runtime，發現任何機器路徑就
  讓建置失敗

### 合規

公開發行前把剩下的授權 gate 全部走完：

- **FFmpeg 從 GPLv3 換成 LGPLv3 build**：原本用的是含 x264/x265 的 full build，
  那會讓每次發行 binary 都必須附上完整對應原始碼。但本專案只拿 FFmpeg 把輸入
  解碼成 PCM（`-vn -ac 2 -ar 44100 -c:a pcm_f32le`），匯出是 JUCE 自己寫 WAV，
  沒有用到任何 GPL-only 元件。改用 BtbN 的
  `ffmpeg-master-latest-win64-lgpl-shared`（有 `--enable-version3`，沒有
  `--enable-gpl`／`--enable-nonfree`），義務降為 LGPL，發行包也再小 19 MiB
- **NVIDIA redistributables 明確化**：GPU 版內嵌 2.46 GiB 的 cuBLAS／cuDNN／
  cuFFT 等 22 個 NVIDIA 函式庫（取自官方 PyTorch wheel，未經修改）。這些適用
  NVIDIA 自己的 CUDA EULA 與 cuDNN SLA，而非本專案的 AGPL 或 PyTorch 的 BSD ——
  torch 的 LICENSE 只涵蓋 NVIDIA 貢獻給 PyTorch 的原始碼。新增
  `NVIDIA-CUDA-NOTICE.txt` 列出確切檔案並指向兩份條款
- **LGPL 元件的義務**：FFmpeg、libsndfile（經 soundfile）、libsoxr（經 soxr）
  三者皆為 LGPL 且都是動態載入，使用者可自行替換，符合 LGPL 要求；notices
  記錄其上游來源
- **notices 重新稽核**：原稽核日期早於 RoFormer 整合，PyTorch／NumPy／PyYAML／
  tqdm／einops／julius／PyInstaller／CPython 版本全部過時，且缺少 librosa 拉進
  的十餘個相依。改由 `tools/collect_runtime_licenses.py` 從凍結該 runtime 的
  直譯器動態收集，版本不可能再與實際出貨的 wheel 脫節
- **安裝程式補上** `AppPublisherURL`／`AppSupportURL`／`AppUpdatesURL`，同時作為
  binary 的 AGPL 對應原始碼位置
- **移除建置機器的個人資訊**：使用者名稱與電腦名出現在 4 個受追蹤檔案與 12 個
  commit 的歷史中，已於 HEAD 與整個 git 歷史清除；兩個測試檔改讀 `HTFX_PYTHON`
  而非寫死只存在於單一機器的直譯器路徑

- `THIRD_PARTY_NOTICES.md` 的模型權重再散布一項結案：Demucs 與 99 個 RoFormer
  checkpoint 一律不隨任何發行物散布，全部於首次使用時從各自上游下載並驗
  SHA-256，因此不涉及再散布授權

## [0.0.1] - 2026-08-30

第一個標記版本。在既有的 HTDemucs 原型上加入 MelBand RoFormer 模型家族、
模式優先的操作介面、多檔批次流程與中英雙語介面。

### 新增

- **MelBand RoFormer 整合**：99 個模型（人聲／伴奏／卡拉 OK／吉他／去混響／
  去噪／群聲／氣音等 10 類），其中 57 個經逐一端到端驗證，其餘標註 experimental
- **模式優先介面**：12 種分離模式，各自帶預設模型並可換同類替代；選定模式後
  拉桿才啟用，且拉桿名稱隨模式改變
- **多檔批次流程**：多選匯入、每檔一列（勾選框／檔名／狀態）、點列切換到該檔的
  調音介面、每檔獨立記住拉桿設定、逐一推論（先完成先可預覽）、只匯出勾選的檔案
  並一次輸出到指定資料夾
- **按需下載**：選用的 RoFormer 模型自動下載並驗證 SHA-256，滾動快取上限 3 個
- **中英雙語介面**：預設繁體中文，可即時切換英文並記住選擇
- **啟動預設**：預選「人聲分離」模式，上次的模式與模型於下次啟動還原
- **macOS 建置支援**：universal binary（Intel ＋ Apple Silicon）設定與
  `tools/build_macos.sh`，說明見 `docs/MACOS_BUILD.md`
- **端到端驗收工具** `tests/goal_check.cpp`：對一首真實歌曲跑完四種分離模式的
  「匯入→分離→匯出人聲→匯出伴奏」

### 修正

- 快速匯出會強制把模型切回 `htdemucs`，與模式優先介面衝突，導致在任何 RoFormer
  模式下按匯出都等不到結果
- `isRoformerModelName()` 只比對 `melband-roformer-` 前綴，使 99 個模型中的 74 個
  （`roformer-model-*`）被誤判為 HTDemucs 模型而拒絕分離
- `SpscRing` 在編譯期值初始化約 75 MB 儲存區，造成編譯器堆積耗盡（C1060），
  並在每次啟動時白白歸零這些頁面
- 快速匯出僅支援 HTDemucs 4/6 軌結果，RoFormer 的 2 軌結果無法匯出
- RoFormer 模型未安裝時被前置檢查擋下，未讓 worker 走按需下載
- CPU 模式警告訊息未納入中文化
- 各分離模式切換時，音軌拉桿名稱停留在前一個模式（例如仍顯示鼓／貝斯）

### 效能

- 5 分鐘歌曲的分離時間：HTDemucs 4 軌 43→16 秒、RoFormer 人聲 83→63 秒
